# Bash Shell Configuration
# Bash: stands for Bourne-again shell

# Check your current shell, output the path to the current shell:

echo $SHELL

# Bash shell = /bin/bash
# 💡 Note the original shell for future reference.
# You can change your shell by running:

chsh -s /bin/bash

# Restart the terminal, this will set your default shell to bash.








